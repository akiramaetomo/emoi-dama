{
  "name": "happy-ball-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:lan": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "tsc --noEmit && tsc -p tsconfig.test.json && node .test-build/packet.test.js && node .test-build/storage.test.js && node .test-build/categories.test.js && node .test-build/settings.test.js && node .test-build/json-transfer.test.js && node .test-build/device-gravity.test.js && node .test-build/visibility.test.js && node .test-build/qr-code.test.js && node .test-build/display-period.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "@dimforge/rapier2d-compat": "^0.19.3"
  }
}
